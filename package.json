{
  "name": "reyce-server",
  "version": "1.0.0",
  "description": "REYCE — Serveur de réservation avec acompte Stripe",
  "main": "server.js",
  "scripts": {
    "postinstall": "prisma generate",
    "start": "(prisma migrate resolve --rolled-back 20260101000001_no_overlap_constraint || true) && prisma migrate deploy && node prisma/seed.js && node server.js",
    "dev": "nodemon server.js",
    "db:migrate": "prisma migrate dev",
    "db:seed": "node prisma/seed.js"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "multer": "^2.2.0",
    "prisma": "^6.19.3",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
